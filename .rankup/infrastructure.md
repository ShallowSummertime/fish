# Infrastructure

- Production domain: `howtofishwalkthrough.com` (content target only; DNS and hosting unverified).
- Build output: `dist/` static files, including per-route `index.html` files generated by `scripts/prerender.ts`.
- Deployment requirement: preserve static files first, then apply an SPA fallback for unknown URLs as described in `DEPLOYMENT.md`.
- Cloudflare resources: unknown; none created in this task.
