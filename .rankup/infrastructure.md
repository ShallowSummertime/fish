# Infrastructure

- Production domain: `https://howtofishwalkthrough.com` (Vercel project `lik/fish`, Production).
- Alternate host: `https://www.howtofishwalkthrough.com` redirects permanently (308) to the apex domain.
- DNS: Namecheap Advanced DNS; apex A record points to `216.198.79.1`, and `www` CNAME points to the project-specific Vercel DNS target.
- Build output: `dist/` static files, including per-route `index.html` files generated by `scripts/prerender.ts`.
- Deployment behavior: eight prerendered routes are served as static HTML; unknown URLs return a real 404.
- TLS and domain configuration: Vercel-managed HTTPS; both custom domains show Valid Configuration.
- Cloudflare resources: unknown; none created in this task.
